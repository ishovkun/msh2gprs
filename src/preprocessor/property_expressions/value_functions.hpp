#pragma once

namespace property_functions {

// truncates the value of x to be within the range [xmin, xmax]
double clip(const double x, const double xmin, const double xmax);

}  // end namespace property_functions
